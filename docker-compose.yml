services:
  tts:
    build: .
    ports:
      - "8765:8765"
    env_file:
      - .env
    volumes:
      - tts-data:/data
    environment:
      TTS_DATA_DIR: /data
      TTS_HOST: 0.0.0.0
      TTS_PORT: 8765

volumes:
  tts-data:
